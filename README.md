# Task Controller

Projeto de uma aplicação para gerenciamento de tarefas.
Criado utilizando React e utilizando o json-server para simular uma API.

### Pré-requisitos

Antes de começar vai ser necessário ter as seguintes ferramentas instaladas no seu ambiente:

[Git](https://git-scm.com/downloads), [Node](https://nodejs.org/en/).

### 🎲 Rodando o projeto.

```
Clone o repositório:

$ git clone https://github.com/otarampinelli/task-controller.git

Rode o comando npm install na raiz do projeto para instalar as dependências:

$ npm install

Instale o json-server de forma global com o comando:

$ npm install -g json-server

Para inciar o projeto rode o comando:

$ npm start

Para inciar o json-server rode o comando:

$ json-server --delay 500 db.json
```


